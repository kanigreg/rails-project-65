DEFAULTS: &DEFAULTS
  title: $LABEL title
  description: $LABEL description
  user: one
  category: cars

one:
  <<: *DEFAULTS
  state: published

two:
  <<: *DEFAULTS
  category: electronics
  state: published

draft:
  <<: *DEFAULTS
  state: draft

under_moderation:
  <<: *DEFAULTS
  state: under_moderation
